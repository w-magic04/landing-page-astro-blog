<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<title>Winter Magic</title>
        <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@700;900&family=Inter:wght@400;700&display=swap" rel="stylesheet">
	</head>
	<body>
        <div class="frosted-border"></div>

		<div class="landing-container">
            <header>
                <div class="logo">
                    <span class="winter-text">Winter</span>
                    <span class="magic-text">Magic</span>
                </div>
                <nav>
                    <ul>
                        <li><a href="#" class="active">Discover</a></li>
                        <li><a href="#">Gallery</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </nav>
            </header>

            <main>
                <div class="top-content-area">
                    <h2 class="subtitle">WINTER MAGIC WILL BE HERE SOON.</h2>
                </div>

                <div class="middle-spacer"></div>

                <div class="bottom-content-area">
                    <p class="description">Take your rewards and wait for us to arrive!</p>
                    
                    <div class="button-wrapper">
                        <a href="https://gift.wintermagic.site" class="get-now-link">GET NOW</a>
                    </div>
                </div>
            </main>
		</div>
	</body>
</html>

<style>
    /* الإعدادات الأساسية */
    :root {
        --gold-bright: #fde2a6;
        --gold-deep: #b88f34;
        --text-white: #f8f9fa;
        --font-serif: 'Cinzel', serif;
        --font-sans: 'Inter', sans-serif;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        background-image: url('/santa.jpg.png'); 
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        min-height: 100vh;
        font-family: var(--font-sans);
        color: var(--text-white);
        overflow: hidden;
        position: relative;
    }

    .frosted-border {
        position: fixed;
        top: 0; left: 0; right: 0; bottom: 0;
        pointer-events: none;
        border: 15px solid rgba(255, 255, 255, 0.08);
        box-shadow: inset 0 0 40px rgba(255, 255, 255, 0.15);
        z-index: 100;
    }

    .landing-container {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        background: radial-gradient(circle, rgba(0,0,0,0) 50%, rgba(10, 23, 46, 0.2) 100%);
        padding: 2rem 4rem;
    }

    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        z-index: 10;
    }

    .logo {
        display: flex;
        flex-direction: column;
        align-items: center;
        line-height: 0.7;
    }

    .logo .winter-text {
        font-family: var(--font-serif);
        font-size: 2rem;
        font-weight: 700;
    }

    .logo .magic-text {
        font-family: var(--font-serif);
        font-size: 1.5rem;
        color: var(--gold-bright);
        margin-left: 2rem;
    }

    nav ul {
        display: flex;
        gap: 2.5rem;
        list-style: none;
    }

    nav a {
        color: var(--text-white);
        text-decoration: none;
        font-size: 1rem;
        transition: 0.3s;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.8);
    }

    nav a:hover, nav a.active {
        color: var(--gold-bright);
        border-bottom: 2px solid var(--gold-bright);
    }

    /* تقسيم الشاشة الرئيسي لتوزيع النصوص */
    main {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between; /* يوزع المحتوى بين أعلى وأسفل تلقائياً */
        text-align: center;
        padding-bottom: 4.5rem; /* رفع المحتوى السفلي قليلاً ليتطابق مع المستطيل */
    }

    /* 1. ستايل العنوان العلوي (فوق رأس سانتا كلوز) */
    .top-content-area {
        z-index: 10;
        margin-top: 1.5rem; /* مسافة خفيفة أسفل الهيدر */
        width: 100%;
        max-width: 900px;
    }

    .subtitle {
        font-family: var(--font-serif);
        font-size: 2.3rem; /* تكبير الحجم ليصبح بارزاً */
        font-weight: 900;  /* خط خشن وعريض جداً */
        letter-spacing: 2px;
        /* تدرج ذهبي ملكي ومزخرف */
        background: linear-gradient(to bottom, #fff3d1, #d4af37, #aa7c11);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        /* إضافة ظل قوي ليفصل الخط عن النجوم والخلفية بوضوح */
        filter: drop-shadow(0px 4px 8px rgba(0, 0, 0, 0.95)) drop-shadow(0px 2px 3px rgba(0, 0, 0, 0.9));
        line-height: 1.2;
    }

    /* المساحة الفارغة في المنتصف لإظهار سانتا بالكامل */
    .middle-spacer {
        flex: 1;
        min-height: 25vh;
    }

    /* 2. ستايل المحتوى السفلي (في الجليد) */
    .bottom-content-area {
        z-index: 10;
        width: 100%;
        max-width: 800px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.8rem;
    }

    /* جعل هذه الكتابة باللون الأسود تماماً وبخط واضح فوق الثلج */
    .description {
        font-size: 1.25rem;
        color: #000000; /* لون أسود خالص */
        font-weight: 700;  /* زيادة سمك الخط لتسهيل القراءة فوق الثلج */
        margin-bottom: 0.5rem;
        letter-spacing: 0.5px;
    }

    /* تنسيق زر GET NOW الذهبي */
    .get-now-link {
        display: inline-block;
        background: linear-gradient(to bottom, #ecd093, #b68d3a);
        color: #fff;
        text-decoration: none;
        padding: 0.8rem 3.5rem; /* حجم طبيعي وأنيق وليس عريضاً */
        border-radius: 50px;
        font-weight: 700;
        font-size: 1.05rem;
        letter-spacing: 1px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.2);
        text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
    }

    .get-now-link:hover {
        transform: translateY(-3px);
        filter: brightness(1.1);
        box-shadow: 0 8px 25px rgba(182, 141, 58, 0.5);
    }

    /* التجاوب مع الجوال */
    @media (max-width: 768px) {
        .landing-container { padding: 1.5rem; }
        header { flex-direction: column; gap: 1rem; }
        nav ul { gap: 1.2rem; }
        .subtitle { font-size: 1.4rem; font-weight: 700; }
        .description { font-size: 1rem; color: #111; }
        main { padding-bottom: 2.5rem; }
        .get-now-link { padding: 0.7rem 2.2rem; font-size: 0.95rem; }
    }
</style>
